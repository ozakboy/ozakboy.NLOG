; Windows 上避免 pnpm symlink 的 EBUSY 錯誤(防毒/編輯器鎖檔)
; Use a flat node_modules layout (no symlinks) — same shape as npm/yarn classic.
node-linker=hoisted

; 嚴格驗證 lockfile,CI 用 --frozen-lockfile 對齊
strict-peer-dependencies=false
auto-install-peers=true
