; npm 專案,使用預設行為。
; 保留此檔僅為固定 registry 與基本設定,避免不同環境差異。
; This project uses npm. Settings here are minimal and pnpm-agnostic.

; 讓 npm 對 peer-dependency 衝突更寬容(避免 Nuxt + Vue 等 ecosystem peer 警告卡住)
legacy-peer-deps=false
